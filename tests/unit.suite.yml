# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - Asserts
        - \Helper\Unit
        # - Db
    # config:
        # Db:
            # dsn: 'mysql:host=localhost;dbname=ttci'
            # user: 'root'
            # password: ''
            # dump: '_data/ttci.sql'
            # populate: true
            # cleanup: false
